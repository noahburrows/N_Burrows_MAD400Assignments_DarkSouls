import { FindPlatformPipe } from './find-platform.pipe';

describe('FindPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new FindPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
